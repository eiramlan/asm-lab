{
  "name": "ASM Lab (NASM)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {},
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y nasm make binutils && echo 'Ready.'",
  "customizations": {
    "vscode": {
      "extensions": ["ms-vscode.makefile-tools"]
    }
  }
}
